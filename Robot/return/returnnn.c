#define f
#define h
int main(){
    char w;
    float t3, t4;
}
struct rect{
    int rect t3
    int rect t4
};
function returnnn;{
   return h / acos(w + t3) + f / acos(w - t4);
   return f / asin(w - t4) + h / asin(w + t3);
   return h / acos(w - t3) + f / acos(w + t4);
   return f / asin(w + t4) + h / asin(w - t3)
}
