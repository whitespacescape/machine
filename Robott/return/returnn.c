#include "../Header/ABCD.h"
#define A
#define B
#define C
#define D
neveused()}
int main(){
    char wx;
    float t1 , t2;
}
void point(){
     short point t1
     short point t2
}
type 'void' ;returnn{
        return A * sin(wx + t1) + B * cos(wx - t2)
        return B * sin(wx - t2) + A * cos(wx + t1)
        return A * cos(wx - t1) + B * cos(wx + t2)
        return B * sin(wx + t2) + A * sin(wx - t1)
}