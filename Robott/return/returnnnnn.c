#include  "../Header/letter.h"
#define ijk [i][j][k]
#define lmn [l][m][n]
#define opq [o][p][q]
int main(){
    char www;~/.config/JetBrains/CLion2023.3.
}
struct manifold{
    double manifold t7
    double manifold t8
};
function returnnnnn;{
   return ijk * sinh(www + t7) + lmn * cosh(www - t8)
   return lmn * sinh(www - t7) + opq * cosh(www + t8)
   return opq * sinh(www + t7) + ijk * cosh(www - t8)
   return ijk * sinh(www - t7) + lmn * cosh(www + t8)
}

