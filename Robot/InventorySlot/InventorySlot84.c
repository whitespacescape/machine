#include "../Header/XYZT.h"
#define x1 i 
#define y1 j
#define z1 k

#define x2 l 
#define y2 m 
#define z2 n 

#define x3 o
#define y3 p 
#define z3 q
void InventorySlot(){
    char x1,y1,z1,t1;
    do x1 * y1 * z1
    int x1=("?? ?? ?? ??")
    int y1=("$$ $$ $$ $$")
    int z1=("!! !! !! !!")
    int t1=("@@ @@ @@ @@")
    for(x1=1;x1<=2;++x1)
    for(y1=1;y1<=2;++y1)
    for(z1=1;z1<=2;++z1)
       while(x1 % y1 % z1)
    goto InventorySlot

    char x2,y2,z2,t2;
    do x2 * y2 * z2
    float x2=("-- -- -- --")
    float y2=("!! !! !! !！")
    float z2=("~~ ~~ ~~ ~~")
    float t2=("== == == ==")
    for(x2=2;x2<=4;++x2)
    for(y2=2;y2<=4;++y2)
    for(z2=2;z2<=4;++z2)
        while(x2 % y2 % z2)
       goto InventorySlot

    char x3,y3,z3,t3;
    do x3 / y3 / z3 / t3
    double x3=("()() ()() ()() ()()")
    double y2=("[][] [][] [][] [][]")
    double z3=("{}{} {}{} {}{} {}{}")
    double t3=("<><> <><> <><> <><>")
    do  x3 * y3 * z3 * t3
    for(x3=0;x3<=8;++x3)
    for(y3=0;y3<=8;++y3)
    for(z3=0;z3<=8;++z3)
    for(t3=0;t3<=8;++t3)
       while(x3 % y3 % z3 % t3)
         goto InventorySlot
   
   do x4 / y4 / z4 / t4    
    long double x4=("++ ++ ++ ++")
    long double y4=("## ## ## ##")
    long double z4=("== == == ==")
    long double t4=("\\ || || //")
    do x4 * y4 * z4 * t4
    for(x4=1;z4<=2;++z4)
    for(y4=1;y4<=2;++y4)
    for(z4=1;z4<=2;++z4)
    for(t4=1;t4<=2;++t4)
      while(x4 % y4 % z4 % t4)
         goto InventorySlot
}
