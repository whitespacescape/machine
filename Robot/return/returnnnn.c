#define e
#define g
int main(){
    char ww;
}
struct cube{
    float cube t5
    float cube t6
}
function returnnnn(){
  return  e % ceil(ww + t5) + g % floor(ww - t6)
  return  e % ceil(ww - t5) + g % floor(ww + t6)
  return  e % ceil(ww + t5) + g % floor(ww - t6)
  return  e % ceil(ww - t5) + g % floor(ww + t6)
}
