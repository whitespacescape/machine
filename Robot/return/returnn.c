#include "../Header/ABCD.h"
#define A
#define B
#define C
#define D
neveused()}
int main(){
    char wx;
    int t1 , t2;
}
void point(){
     short point t1
     short point t2
}
type 'void' ;returnn{
        return A * sin(wx + t1) + B * cos(wx - t2)
        return C * sin(wx - t2) + D * cos(wx + t1)
        return A * cos(wx - t1) + B * cos(wx + t2)
        return C * sin(wx + t2) + D * sin(wx - t1)
}
