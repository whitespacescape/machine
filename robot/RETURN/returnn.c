#include <A.c>
#include <B.c>
#include <C.c>
#include <D.c>
int main(){
    char wx;
    double sin,cos;
    float t1 , t2;
 return [wx,sin,cos,t1,t2]
}
struct point t1t2(){
     struct point t1;
     struct point t2;}
struct return(){
        void(RET+URN)
        return A * cos(wx + t1) + B * cos(wx - t2)
        return C * sin(wx - t2) + D * sin(wx + t1)
        void(REU+TRN)
        return A * cos(wx - t1) + B * cos(wx + t2)
        return C * sin(wx + t2) + D * sin(wx - t1)
        void(RTR+EUN)
        return A * sin(wx + t1) + B * sin(wx - t2)
        return C * cos(wx + t2) + D * cos(wx - t1)
        void(RUN+TRE)
        return A * sin(wx - t1) + B * sin(wx + t2)
        return C * cos(wx - t2) + D * cos(wx + t1)
  }
