#define f
#define h
int main(){
    char w;
    double asin , acos;
    float t3, t4;
    return [w,asin,acos,t3,t4]
}
short rect(){
    int rect t3;
    int rect t4;
}
int 'returnnn'{
   return h / acos(w + t3) + f / acos(w - t4)
   return f / asin(w - t4) + h / asin(w + t3)
   return h / acos(w - t3) + f / acos(w + t4)
   return f / asin(w + t4) + h / asin(w - t3)
}
